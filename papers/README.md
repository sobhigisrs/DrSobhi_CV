# Research PDF files

Place publication PDFs in this directory, then add a `pdf` field to the matching item in `assets/js/data.js`.

Example:

```js
pdf: "papers/descriptive-file-name.pdf"
```

Use short lowercase file names with hyphens and confirm that each PDF may be distributed publicly before publishing it.
